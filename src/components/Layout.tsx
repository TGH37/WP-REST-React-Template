import React from 'react';
import Navbar from './Navbar';

interface Props {};

function Layout(props: Props) {
    const {} = props;

    return (
        <Navbar />
    );
};

export default Layout;
